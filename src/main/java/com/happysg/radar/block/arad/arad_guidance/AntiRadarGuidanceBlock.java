package com.happysg.radar.block.arad.arad_guidance;

public class AntiRadarGuidanceBlock {
    // like the Radar guidance block, but can only target radars
    // will ignore radars that are; not active, within a certain range, etc
    // invisible to RWR
    // if radar source is lost, it will detonate OR flail wildly OR target last known position

}
